import {
  ReactElement,
  ReactNode,
  ReactDOM,
  ReactComponentElement,
  ReactHTMLElement,
  ReactPropTypes,
} from 'react';

export default async function RooyLayout({ children }: any) {
  return <>{children}</>;
}
